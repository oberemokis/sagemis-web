services:
  web:
    build: .
    image: sagemis-web
    container_name: sagemis-web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /home/sagemis/certs:/etc/nginx/certs:ro
    restart: unless-stopped
